import { Manrope, Ubuntu } from "next/font/google";

export const manrope = Manrope({
  weight: ["300", "400", "700"],
  subsets: ["latin"],
});

export const ubuntu = Ubuntu({
  weight: ["400", "500", "700"],
  subsets: ["latin"],
});
